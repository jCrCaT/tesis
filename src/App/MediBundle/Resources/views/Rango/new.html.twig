{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Rango creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_rango') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
